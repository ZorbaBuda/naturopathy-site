import { servicesVisits } from "@/lib/data"

const ServFunctioningSection = () => {
  return (
    <section className="section w-full bg-grey_1">
      <div className="subcontainerXl">
        <h2 className="h2">{servicesVisits.title}</h2>

      </div>

    </section>
  )
}

export default ServFunctioningSection